---
import EventsSection from '../components/EventsSection.astro';
// import FeaturedProducts from '../components/FeaturedProducts.astro';
import Hero from '../components/Hero.astro';
import OfferingSection from '../components/OfferingSection.astro';
import WhySection from '../components/WhySection.astro';
import Layout from '../layouts/Layout.astro';
// import { getFeaturedProducts } from '../lib/shopify';
import { todayInBucharest } from '../lib/events';
import { loadQuery } from '../sanity/lib/load-query';

// Rendered on demand (ISR, see astro.config.mjs) so the upcoming-events
// filter below is re-evaluated against the real date instead of build time.
export const prerender = false;

const HOME_QUERY = `*[_type == "homePage" && _id == "homePage"][0]{
	hero {
		video {
			"playbackId": asset->playbackId
		}
	},
	offering {
		video {
			"playbackId": asset->playbackId
		}
	}
}`;

const { data: homePage } = await loadQuery<{
	hero: {
		video?: { playbackId?: string };
	};
	offering?: {
		video?: { playbackId?: string };
	};
}>({ query: HOME_QUERY });

const today = todayInBucharest();

const EVENTS_QUERY = `*[_type == "event" && date >= $today] | order(date asc){
	_id,
	title,
	"slug": slug.current,
	date,
	"summary": pt::text(description)
}`;

const { data: events } = await loadQuery<
	{
		_id: string;
		title: string;
		slug: string;
		date: string;
		summary?: string;
	}[]
>({ query: EVENTS_QUERY, params: { today } });

// const products = await getFeaturedProducts(4);
---

<Layout
	title="Noima — Prăjitorie de cafea de specialitate în Cluj"
	description="Cafea de specialitate prăjită în Cluj-Napoca. Single origin și blend-uri locale, prăjite proaspăt de oameni care trăiesc din cafea de 18 ani. Magazin online și cafenea pe Strada Bobâlnei 58."
>
	<Hero hero={homePage?.hero ?? {}} />
	<WhySection />
	{/* <FeaturedProducts products={products} /> */}
	<OfferingSection offering={homePage?.offering} />
	<EventsSection events={events ?? []} />
</Layout>
