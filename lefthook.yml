pre-commit:
  jobs:
    - name: biome format
      glob: "*.{js,ts,jsx,tsx,mjs,cjs,json,css}"
      exclude:
        - "noima-coffee-theme/**"
        - ".claude/**"
        - ".agents/**"
        - "skills.lock"
      run: pnpm biome check --fix {staged_files} && git add {staged_files}
