# TODO

Running list of follow-ups for the noima site.

## Pending

- [ ] **Re-enable `FeaturedProducts` on the home page.** Temporarily commented out in [src/pages/index.astro](src/pages/index.astro) along with the `getFeaturedProducts` import and `products` query. Uncomment the import, the `getFeaturedProducts(4)` call, and the `<FeaturedProducts products={products} />` render when the Shopify catalog is ready.
